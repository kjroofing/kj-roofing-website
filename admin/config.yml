backend:
  name: git-gateway
  branch: main

media_folder: "images"

collections:
  - name: "gallery"
    label: "Gallery"
    folder: "images"
    create: true
    fields:
      - {label: "Image", name: "image", widget: "image"}
